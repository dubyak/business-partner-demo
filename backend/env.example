# Anthropic API Configuration
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Langfuse Configuration (Optional but recommended for observability)
LANGFUSE_SECRET_KEY=sk-lf-your_secret_key_here
LANGFUSE_PUBLIC_KEY=pk-lf-your_public_key_here
LANGFUSE_BASE_URL=https://cloud.langfuse.com
# LANGFUSE_PROMPT_NAME=business-partner-system  # Optional: Name of prompt in Langfuse (defaults to 'business-partner-system')

# LangSmith Configuration (Optional but recommended for observability)
LANGSMITH_API_KEY=your_langsmith_api_key_here
LANGSMITH_PROJECT=business-partner-demo
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com  # Optional: Custom endpoint (defaults to LangSmith cloud)

# Server Configuration
PORT=3000

